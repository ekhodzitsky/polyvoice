# src/earshot_vad — TODO

- [ ] Expand parity notes vs Silero on a fixed Vox/AMI subset before any
      production-default discussion.
- [ ] Wire into AdapterRegistry docs consistently with other VAD adapters.
- [ ] Property test: process returns exactly input.len() / FRAME_SIZE
      probabilities for aligned input and rejects every other length.

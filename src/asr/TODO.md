# src/asr — TODO

- [ ] (companion crate) `polyvoice-asr`: Parakeet TDT via `parakeet-rs` implementing `Asr`.
- [ ] (core) word→speaker join: `Vec<Word>` + turns → `Vec<WordAlignment>` (separate module).
- [ ] Consider a streaming ASR trait variant once a streaming backend exists.

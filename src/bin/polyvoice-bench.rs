//! polyvoice-bench — DER on a {audio,rttm} dataset directory using the v1.0 Pipeline.
//!
//! NOTE: This binary is being rewritten in Task 7 (M6b). This is a temporary
//! stub that compiles under the new API surface so that `cargo build --features cli`
//! succeeds after Task 6.

fn main() {
    eprintln!("polyvoice-bench: Task 7 rewrite pending. Use polyvoice diarize for now.");
    std::process::exit(1);
}

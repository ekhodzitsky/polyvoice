# src/format — TODO

- [ ] WebVTT `<v SPEAKER_NN>` voice tags once ASR text is populated.
- [ ] Optional variants (e.g. merge consecutive same-speaker turns into one cue).
- [ ] CLI `--format srt|vtt|txt` wiring (separate task — keep this module I/O-free).

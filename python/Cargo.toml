[package]
name = "polyvoice-python"
version = "0.6.0-alpha.0"
edition = "2024"
publish = false

[lib]
name = "_polyvoice"
crate-type = ["cdylib"]

[dependencies]
polyvoice = { path = "..", features = ["onnx", "pipeline", "segmentation", "embedder", "clusterer", "resegmentation"] }
pyo3 = { version = "0.28", features = ["extension-module"] }
